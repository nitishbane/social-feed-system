{
  "name": "social-feed-system",
  "version": "1.0.0",
  "description": "Baseline social feed system API in Node.js with PostgreSQL schema.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "check": "find src scripts -name '*.js' -print0 | xargs -0 -n1 node --check",
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
